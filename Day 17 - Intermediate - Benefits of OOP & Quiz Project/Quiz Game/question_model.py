class Question:

    def __init__(self, text, answer):
        """Creates and initializes an object that accepts two arguments, the question text as the first attribute and the answer as the second."""
        self.text = text
        self.answer = answer